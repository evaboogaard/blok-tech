{{>header}}

<ul>
    <li>{{name}}</li>
    <li>{{email}}</li>
    <li>{{country}}</li>
</ul>

<form method='POST' action="/delete" id="delete">
    <button type="submit" name="deleteId" value="{{this._id}}">Delete Account</button>
</form>

{{>nav}}